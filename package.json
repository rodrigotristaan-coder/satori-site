{
  "name": "satori-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing + core web de SATORI — build con esbuild (React de produccion, sin Babel en el navegador)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx --yes serve dist -l 4321"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
